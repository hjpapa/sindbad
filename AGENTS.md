# AGENTS.md — 신밧드: 일곱 보물과 바다의 약속

## 작업 진입점

- 상세 요구사항의 기준은 `SINBAD_GAME_SPEC.md`다. 첫 작업에서는 전체 구조와 0장·14장·15장을 읽고, 현재 구현할 스테이지·시스템을 이어 읽는다. 긴 파일은 구간을 나누어 읽어 생략하지 않는다.
- 매 작업 시작 시 기존 코드, 이 파일, `PROJECT_STATUS.md`를 확인한다. 사용자 변경을 지우거나 다른 프로젝트로 덮어쓰지 않는다.
- 새 프로젝트의 기본은 Phaser 3.90.0 + TypeScript strict + Vite다. 엔진 3/4 API를 섞지 않는다. 의존성·Node·OS 지원 범위를 확인하고 lockfile을 유지한다.
- 실시간 월드·물리는 Phaser, 메뉴·대화는 DOM 오버레이. 로그인·DB·외부 AI API·광고·결제를 도입하지 않는다.

## 범위와 정직한 상태 관리

- 최종 목표는 36개 스테이지, 아이의 원래 장면 24개 전부 반영, 무기 7종·핵심 보물 7개다.
- 최초 작업 범위는 M0+M1, S01~S03의 실제 플레이 구현과 검증이다. 이후 `PROJECT_STATUS.md`에서 다음 미완료 마일스톤을 이어 간다.
- 콘텐츠 등록(planned), 실제 구현(implemented), 플레이 검증(verified)을 구별한다. 복사 맵·빈 맵을 완료로 처리하지 않는다.
- 설계서만 출력하고 끝내지 말고 요청된 범위의 실제 코드를 만든다. 실행하지 않은 검사를 통과했다고 쓰지 않는다.
- 웹툰 아트는 최종 요구사항이다. 이미지가 없으면 실제 존재하는 간이 에셋으로 기능을 구현하고 ART_DRAFT로 기록한다. 없는 파일·완성 아트를 꾸며내지 않는다.

## 놓치면 안 되는 규칙

- 하트 회복·레벨업·무기 교체·NPC 대화·보물 능력이 실제 플레이에서 작동해야 한다.
- 핵심 보물은 확정 획득이며, 필요한 능력보다 먼저 이를 요구하는 관문을 두지 않는다. S05의 공기방울과 S16의 진주를 구별한다.
- 보상 ID·아이템 ID는 안정적으로 유지한다. 같은 보상은 한 번만 지급하며 저장·재시도·대화 생략에 결과가 일치해야 한다.
- 사망 때 진행·보물을 잃지 않고 안전 체크포인트에 완전 회복하여 재시작한다.
- 인간 적은 항복, 동물은 저주 해제, 마법 적은 빛·연기로 표현한다. 피·절단·고문·신격 희화화는 넣지 않는다.
- 기존 작품의 이미지·대사·음악을 복제하지 않는다. 자산의 실제 경로·출처·이용 조건을 기록한다.
- 공개 배포, 기존 배포 교체, 유료 서비스, 계정 변경은 별도 요청 없이 수행하지 않는다.

## 검증과 마무리

실제 저장소에 다음 명령을 제공하고 변경 범위에 맞게 실행한다.

```text
npm run typecheck
npm run lint
npm run test
npm run validate:content
npm run build
npm run test:e2e
```

브라우저 실행이 가능하면 정상 조작으로 현재 구간을 플레이하고 콘솔 오류·스크린샷·체크포인트 재개를 확인한다. 실행 환경 때문에 못 한 검사는 미검증 사유를 남긴다.

매 작업 끝에 `PROJECT_STATUS.md`의 구현 범위·실제 검사 결과·남은 문제·다음 한 작업을 갱신한다. 기능 완료와 최종 아트 완료를 별도로 기록한다.
