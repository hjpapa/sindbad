# 플레이 검증 기록

빌드 0.1.0 / 2026-09-06 / Windows 10.0.26200, Node 24.16.0 / Playwright 1.55.1, 설치된 Microsoft Edge / 데스크톱 1280×720, 모바일 모사 844×390.

## 방법과 경계

`tests/e2e/adventure.spec.ts`는 새 저장에서 실제 키보드로 이동·점프·공격·대화·보물을 조작합니다. 개발용 API는 위치·HP·보상 상태의 **읽기 전용 스냅샷**이며 테스트가 플레이어 위치나 보상을 직접 변경하지 않습니다. 사망/복구 등의 독립 회귀 검사는 별도 초기 저장 픽스처를 사용하므로 캠페인 완주 증거와 구분합니다.

앱 내 브라우저 스킬 연결은 `privileged native pipe bridge is not available; browser-client is not trusted`로 실패했습니다. 별도 UI 자동화의 Edge 연결도 사용할 수 없었습니다. 저장소 Playwright에서 설치된 Edge headless 실행은 가능했고, 생성된 스크린샷을 직접 열어 확인했습니다. 따라서 **도구를 통한 실제 입력 자동 플레이 + 스크린샷 육안 확인**이며 별도 손 플레이나 실제 모바일 기기 검증으로 보고하지 않습니다.

## 확인한 화면

- `screenshots/title.png`: 제목, 이어하기 비활성화 이유, 설정·파일 보관, ART_DRAFT 표시.
- `screenshots/harbor.png`: 플레이어/해골/하트/상자 형태, 서로 다른 지형, HUD와 조작 안내.
- `screenshots/mobile-844x390.png`: 터치 버튼 표시 및 동시 입력. 48px 버튼 유지. 캔버스 내부 장치 글씨는 축소되어 최종 모바일 가독성 개선이 필요합니다.
- `screenshots/death-restart.png`: 자연 번개 사망 후 완전 회복, 무기·유물·XP 유지.

## 실제 발견과 수정

1. 타입 검사: JSON의 빈 배열에 대한 union 추론으로 `includes` 인자가 `never`가 됨. 명시적 StageRegistration 타입으로 수정.
2. CSS 로더: 불필요한 data-URL import가 Vite의 파일 경로로 해석됨. 삭제.
3. 콘텐츠 검사: 선택 상자 발판을 필수 바닥으로 분류해 높이 검사가 오탐. 필수 바닥 속성을 분리.
4. E2E: 대화나 무기 교체 입력의 다음 게임 프레임 전에 검사하여 실패. DOM/상태 변경을 기다리도록 수정.
5. E2E: 적이 플레이어 뒤에 있는데 계속 오른쪽으로 공격. 현재 적 위치에 맞춰 실제 방향키를 누르도록 수정.
6. 암초 낙하: 좌표만 옮기던 복귀 코드를 Arcade Body.reset으로 변경해 충돌 몸체도 초기화. 낙하 복귀 전용 회귀 검사 추가.
7. 회피: 최대 수평 속도가 300에 제한되어 있던 것을 회피 중 530으로 변경.
8. 보스: 보상·승리 목표·안전 체크포인트를 단일 순수 상태 전이에서 확정.
9. 세이렌: 파도/음표 투사체에 ≋ / ♪ 형태 표시. 근접 적 예고 범위를 실제 공격 범위와 같은 사각형으로 표시.
10. Windows에서 실패 trace 정리가 오래 대기하여 E2E trace는 껐습니다. HTML 결과와 실패/통과 지점 스크린샷은 유지합니다.

## 범위별 판정

최종 `npm run test:e2e`: 8개 통과 (2.4분). 새 게임 S01~S03 연속 시나리오 1.4분. `docs/VALIDATION_LOG.md`에 실제 로그를 보관했습니다.

| 범위 | 구현 | 검증 |
|---|---|---|
| S01 항구 | 구현 | 통과 · 대화·메달·전투·대장·출항·중간 재접속 |
| S02 암초 | 구현 | 통과 · 세 종·저주 해제·W02 수령/교체/왕복·등대·낙하 복귀 |
| S03 갑판 | 구현 | 통과 · 두 장치·R02·위기 생략·하트·밧줄·자연 번개 사망/복귀 |
| S04 고래섬 | 구현 | 정상 키 입력 · 장비 3개·G01·움직이는 발판·저장 재개 |
| S05 산호 | 구현 | 정상 키 입력 · 열쇠·문·구출·G02·수역 보호·저장 재개 |
| S06~S36 각 단계 | planned, 실제 맵 없음 | 미실행 |

S06~S36은 개별 실기 검사 미실행이며 자동 등록·맵 순간 이동을 플레이 완료로 처리하지 않습니다.

미검증: 실제 태블릿·휴대폰 기기, Safari/Firefox, 게임패드, 장시간 메모리/프레임 성능, 어린이 난이도·소요 시간 평가, 최종 웹툰 아트/배경 음악, S06 이후 보물 능력 실행(자유 수영·비행·마법 다리 등).

## M2 첫 구간 테스트 범위 — 2026-09-08

S04~S05는 기존 M1 저장을 읽은 뒤 정상 키 입력으로 진행한다. 순간이동·보상 지급 테스트 훅을 추가하지 않았다. 기존 S01~S03 새 게임 완주 테스트는 S04 진입까지 확장했고, 별도 이어하기 시나리오는 구명 장비/이동 발판/G01부터 나이라 구출/G02/수역 호흡까지 확인한다. 별도 전투·관문 검사는 방패 앞 공격 차단, 빈틈 타격, 열쇠 없는 상자/왕관 없는 산호문을 확인한다. 이는 S01~S05를 한 번에 새 저장으로 완주한 단일 테스트나 실제 어린이 손 플레이를 대신하지 않는다.

최종 실행: Edge headless 전체 E2E 10개 통과(3.6분). M2 이어하기 시나리오 57.0초, 방패/문 검사 13.6초. 중간 로딩·점프 조사 타이밍 실패를 대기 조건으로 수정한 후 전체 재실행했다. 스크린샷 3개는 docs/screenshots에 보관하고 육안 확인했다.
